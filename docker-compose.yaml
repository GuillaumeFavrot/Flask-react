version: '3.7'

services:
  flask_gunicorn:
    build:
      context: .
    ports:
      - "8000:8000"

  nginx:
    build: ./nginx
    ports:
      - "80:80"
    depends_on:
      - flask_gunicorn

volumes:
  static: